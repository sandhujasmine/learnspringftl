TODO
[_] demonstrate use of UTF-8-encoded i18n files
	try http://forum.springsource.org/showthread.php?p=169307
[_] serve "GET /hello" instead of "GET /hello.ftl"

DONE
[X] basic Maven build
[X] set up Jetty
[X] serve a static page
[X] Spring
[X] Freemarker
[X] pass data to .ftl page
[X] submit form in .ftl page, accept data
[X] remove Mifos-specific code
	instead, just add bits and pieces as necessary
	ie: to test Spring Security, implement that anew
[X] fix "mvn jetty:run" so hello.ftl is shown by default
	instead, made an index.html file
	doesn't expose underlying implementation (Freemarker)
	cleaner URLs
-----------------------------------------------------------------------
vim: ft=vo_base
